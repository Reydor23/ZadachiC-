#include <iostream>
#include <stdio.h>
#include <cmath>

/*int main()
{
	int item;
	printf("Enter Number:");
	scanf_s("%d", &item);
	if (item >=-10 && item <= 10)
	{
		if (item == 0)
		{
			printf("The number is zero!\n");
		}
		if (item < 0)
		{
			printf("The number is negative!\n");
		}
		if (item > 0)
		{
			printf("The number is positive!\n");
		}
	}
	else
	{
		printf("The number does not belong to the interval!\n");
	}
}*/